# v1.161 — 점검 후 수정

- 견적 자동 출고/복구도 재고 revision 조건부 저장 사용. 충돌 시 로컬 재고 유지 및 안내. 복구 실패 시 견적 삭제 중단.
- 구버전 메모에서 quote_id를 보완한 데이터도 불필요한 충돌이 발생하지 않도록 비교 정규화.
- 모바일 품목 옵션을 이름/매입단가/매출단가 카드로 배치. 삭제 터치 영역 44px, 두 단가 칸 동일 폭.
- 입출금 날짜에 연도 포함. 모바일 견적 내역 날짜는 별도 한 줄.
- 모바일 견적 상세에서 목록 CSV 숨김. 목록에서는 유지.
- PC 매출 표 고정 1050px/열 px 폭을 비율 기반으로 변경.

## 검증

9 suites: navigation, materials, quote-workflow, stock-planning, company-address, sales-insight, stock-entry, review-fixes, release. 모두 통과. git diff --check 통과.

합성 데이터 브라우저 실측:
- 390px: 옵션 카드 334px, 가로 넘침 없음. 단가 입력 각 145px, 삭제 44px.
- 320px: 옵션 카드 clientWidth/scrollWidth 모두 264px, 입력/삭제 오른쪽 경계 276px.
- 모바일 견적 CSV display none, 입금 날짜 height 19.5px/white-space nowrap.
- PC 1639px: 매출 표와 부모 모두 977px, 마지막 마진 열까지 표시.
- 콘솔 오류 없음. 실제 데이터 쓰기 및 실물 iPhone 검증은 하지 않음.

## 운영 주의

모든 사용 기기를 새로고침해야 새 저장 보호가 적용됨. 구버전 앱은 기존 덮어쓰기 코드가 남아 있으므로 동시 사용하지 말 것. 이 변경은 재고 저장 경로 보호이며 견적·재고 여러 파일을 하나의 트랜잭션으로 만드는 변경은 아님. 견적 저장 후 재고 반영 실패 안내가 나오면 동기화 후 해당 견적을 다시 저장해야 함.

복구 기준 v1.160. 코드 복구는 새 PR/캐시 버전으로 수행하며 업무 데이터 복원과는 별개.
