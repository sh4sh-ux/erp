# ERP 개선 로드맵 — 이카운트 ERP 대비 분석

> 작성: 2026-08 (v1.25 기준) · 근거: 이카운트 기능 조사 + 현재 코드 실측 감사
> 요약본은 `CLAUDE.md`의 "알려진 결함(미수정)" 참조. 이 파일이 전체 판단 근거다.

## 결론

현재 앱은 ERP가 아니라 **견적서 발행기 + 장부 메모**다. 1인 사업에 이카운트는 과하므로
그 자체는 문제가 아니다. 진짜 문제는 **재고·미수금이 ERP처럼 보이면서 숫자가 실제로 틀리는 것**.
틀린 숫자는 없는 기능보다 나쁘다. **새 기능보다 정합성 복구가 먼저다.**

---

## 1. 이카운트와의 근본 차이 — 문서가 1개뿐

이카운트: **견적 → 주문 → 판매(출고) → 세금계산서 → 수금**이 각각 별개 전표이고,
한 번 입력하면 재고·미수금·세금계산서로 함께 흐른다.

현재 앱: `Quote` 하나가 네 역할을 겸직하고, 상태값(작성중/발송/수주/취소)이 문서 종류를 대신한다.

| 실무 상황 | 현재 앱 |
|-----------|---------|
| 견적 냈다가 수량 조정돼 주문됨 | 원본 견적을 덮어씀 — **견적 이력 소실** |
| 300장 중 100장만 먼저 납품 | 표현 불가 — 부분 출고 개념 없음 |
| 언제 매출로 확정됐나 | 없음 (`created_at`만, 상태 변경 시각 없음) |
| 세금계산서 발행했나 | 추적 불가 |
| 수주했지만 아직 제작 전 | 재고에서 이미 차감됨 |

**미수금이 "수주 = 매출"로 계산되는 것이 가장 위험** — 아직 납품 안 한 건도 미수금으로 잡혀
실제 받을 돈보다 부풀려 보인다.

---

## 2. 확인된 결함 — ①②③ v1.27에서 수정 완료 ✅

> 아래 ①②③은 **v1.27에서 해결**되었고 같은 시나리오로 재검증했다.
> ④(미수금이 수주 기준)는 납품 문서가 필요해 2단계로 남아 있다.

### 원래 문제 (기록 보존)

`/tmp` 감사 스크립트로 실제 재현한 결과. 추측 아님.

### ① 재고 수량이 3가지 경로로 틀어진다
`submitQuote`의 `wasWon` 가드는 "재저장 시 중복 차감"만 막고, 상태 되돌리기·수량 변경은 처리하지 않는다.

| 시나리오 | 실측 | 정상 |
|----------|------|------|
| 수주 → 취소 | 90 (그대로) | 100으로 복구 |
| 취소 → 다시 수주 | **80 (이중 차감)** | 90 |
| 수주 건 수량 10 → 30 수정 | 출고 기록 10 그대로 | 30 반영 |

### ② 견적번호 중복
`nextQuoteNo`가 "같은 날짜 견적 **개수** + 1"로 채번.
`Q-20260709-1`, `-2` 생성 후 `-1` 삭제 → 다음 견적도 `-2` (실측 확인).

### ③ 거래처 삭제 시 고아 데이터
`deleteCompany`가 연쇄 확인 없이 삭제. 견적서·수금 내역이 남아 "(삭제된 거래처)"로 표시되고
미수금 화면에 유령 거래처로 잡힌다. (실측: 견적 2건·수금 1건 잔존)

---

## 3. 삭제 → **살리기로 결정** (v1.29~v1.31, 2단계 완료 ✅)

> 사용자 확인 결과 **네 항목 모두 실제로 쓰고 있어** 삭제하지 않고 기능을 붙였다.
> - 매입 거래처 구분 → 거래처 목록 구분 필터 (v1.30)
> - 품목 매입단가 → 품목·견적·매출 집계 마진 (v1.29)
> - 지급 내역 → 수금/지급 화면 거래처별 집계 (v1.30)
> - 견적 '발송' 상태 → 발송일 기록 + '발송 N일째' 배지 (v1.30)
> - 대시보드 → 삭제 대신 매출·마진·미수금·회신대기를 담은 현황 화면으로 재구성 (v1.31)

### 원래 판단 (기록 보존)

| 대상 | 실태 | 권고 |
|------|------|------|
| 거래처 `매입` 구분 | 라벨만, 매입 문서 흐름 없음 | 매입 추가하거나 **제거** |
| 품목 `매입단가` | 세트 합계 표시 외 미사용. 마진 분석 없음 | 마진 분석 넣거나 **제거** |
| 수금/지급의 `지급` | 대응 문서 없고 미수금 미반영 | 매입 붙이거나 **제거** |
| 견적 상태 `발송` | 대시보드 카운트 외 동작 없음 | 발송일 기록 넣거나 **제거** |
| **대시보드 탭** | 내용이 이달 수금/지급 + 6개월 현금 차트뿐. 매출·수주 안 보임 | 매출 집계·미수금과 중복 — **정리**, 미수금을 첫 화면으로 |

---

## 4. 추가 (우선순위)

**1단계 — 정합성 복구 ✅ v1.27 완료**
1. ~~재고 출고 분리~~ → **reconcile 방식으로 재설계**. 견적 저장 시 "목표 상태"와 이미 반영된
   양의 차이만 기록하므로 취소 복구·수량 증감·재저장·삭제가 모두 자동으로 맞는다.
   별도 "출고 처리" 버튼 대신 이 방식을 택한 이유: 수주와 출고를 진짜로 나누려면 납품 문서가
   필요한데 그건 2단계 항목이라, 1단계에서는 기존 흐름을 유지한 채 숫자만 정확하게 만들었다.
   `stock_moves.quote_id` 추가는 2단계 납품 문서의 토대가 된다
2. ~~견적번호 채번~~ → **최대 일련번호 +1**로 변경, 삭제 후 중복 해소 ✅
3. ~~거래처 삭제 경고~~ → 연결된 견적·수금 건수와 영향을 알리고 확인 ✅

**2단계 — 실무 공백**
4. **납품(출고) 문서 분리** — 견적과 별개로 언제 무엇을 얼마나 납품했는지. 미수금 정확도 + 부분 납품
5. **수금 ↔ 매출 건 연결** — 현재는 거래처 단위 총액 상계라 어느 건이 미결제인지 모름
6. **세금계산서 발행 여부 체크** — 홈택스 연동은 과함. 발행함/미발행 표시만으로 누락 방지

**3단계 — 여유 시**
7. 품목별 마진 (매입단가를 살릴 경우)
8. 부가세 면세·영세 구분 (현재 10%가 6곳 하드코딩 — 과세 거래만 하면 불필요)

---

## 5. 유지 — 이카운트보다 나은 부분 (건드리지 말 것)

- **서버 없이 본인 Dropbox 저장** — 월 0원, 데이터 주권. 이카운트는 계정 끊기면 접근 불가
- **견적서 카톡 이미지 전송** — 이카운트에 없는 흐름. 가장 자주 쓰는 기능이 가장 빠름
- **품목 색상 × 사이즈 옵션** — 의류·자수업에 정확히 맞는 모델
- **단일 HTML 2,500줄** — 전체를 한눈에 파악 가능. 유지보수의 핵심 자산

---

## 6. 이카운트를 따라가지 말 것 (1인 사업에 과잉)

다중 창고·WMS · 바코드/LOT 추적 · 생산 MRP·작업지시 · 급여/인사 ·
다국어·다통화 · 여신한도 관리 · 결재 워크플로

입력 부담만 늘고 실제로 채워지지 않는다.

---

## 권고 순서

**1단계 없이 3단계를 하면 틀린 숫자 위에 기능을 쌓는 것이 된다.**

1. 재고 출고 로직 분리·수리 → 견적번호 채번 → 거래처 삭제 경고
2. ~~껍데기 필드 정리~~ → **전부 살리기로 결정, v1.29~v1.31 완료** ✅
   (사용자가 네 항목 모두 사용 중이라 삭제 대신 기능 부여)
3. 납품 문서 분리 → 수금-매출 연결 → 세금계산서 체크

## 참고

- [이카운트 영업/판매 모듈](https://www.ecount.com/wmail/kr/erp/sales.aspx)
- [이카운트 전자세금계산서](https://www.ecount.com/kr/ecount/product/accounting_electronic-tax-invoice)
- [ECOUNT ERP 기능 개요](https://www.ecount.com/us/ecount/product/erp_features)
