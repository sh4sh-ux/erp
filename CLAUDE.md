# ERP — 거래처 관리 업무 앱

## 프로젝트 개요
단일 HTML 파일로 동작하는 소규모 사업자용 ERP. dutch-pay·receipt-db의 자매 앱으로
디자인 톤·코드 스타일을 맞춤. 거래처·품목·재고·견적서·수금/지급을 한 곳에서 관리하고,
견적서를 인쇄·이미지·공유로 고객에게 전달하는 것이 핵심 용도.

- 사용자: 한국어 사용자, 비개발자, 혼자 사용 (디에디트 / THE EDIT)
- 데이터는 **사용자 본인 Dropbox에만** 저장 — 별도 서버 없음
- 외부 의존성 없음 — 순수 HTML + CSS + Vanilla JS, 인라인 SVG
- 데스크탑·모바일 양쪽에서 사용 (모바일 비중 높음 — iOS Safari·카카오톡 인앱 브라우저)

## 라이브 URL / 저장소
- **라이브:** https://sh4sh-ux.github.io/erp/
- **GitHub:** https://github.com/sh4sh-ux/erp
- **배포 브랜치:** `main` (GitHub Pages가 main에서 자동 배포, 반영까지 1~2분)

## 파일 구조
```
index.html            — 앱 전체 (HTML/CSS/JS 통합, 약 2,480줄)
sw.js                 — 서비스워커 (오프라인 지원)
manifest.webmanifest  — PWA 매니페스트 (아이콘·테마색)
favicon.png           — 브라우저 탭 아이콘 64px
icons/icon-180.png    — iOS 홈 화면 (apple-touch-icon)
icons/icon-192.png    — 매니페스트 아이콘
icons/icon-512.png    — 매니페스트 아이콘
assets/business-card-gownii.png — 명함 보내기 기본 이미지
CLAUDE.md             — 이 파일 (세션 컨텍스트용)
ROADMAP.md            — 이카운트 ERP 대비 분석 · 개선 우선순위 (추가/유지/삭제 판단 근거)
```

## 버전 관리
- 단일 상수 `APP_VERSION` (JS 상단, `DROPBOX_APP_KEY` 바로 아래)이 진실의 원천.
  `init()`에서 상단바 `#tbVer`에 주입 — DOM에 버전을 하드코딩하지 말 것
- 형식: `v메이저.패치` (예: `v1.9` → `v1.10` → `v1.11`)
- **변경 시마다** `APP_VERSION` + 아래 changelog 한 줄 + 커밋 메시지(`vX.Y: 요약`)를 함께 갱신
- 사용자가 라이브에서 버전 칩으로 배포 반영 여부를 확인하므로 버전 누락 금지

## 데이터 저장 (Dropbox)
- OAuth: Authorization Code + PKCE (`client_secret` 불필요, 서버 없음)
- App key는 `DROPBOX_APP_KEY_DEFAULT`(`uy4mukymihfjf1o`, 접근 유형 **App folder**)가 기본값이고,
  로그인 화면에서 새 키를 넣으면 localStorage `dbx_app_key`가 우선한다 (`appKey()`).
  **`/07_Apps/`에 저장하려면 Full Dropbox 권한의 앱을 새로 만들어 그 키를 넣어야 한다** —
  Dropbox는 앱 생성 후 접근 유형을 바꿀 수 없다.
- 토큰은 localStorage(`dbx_access`/`dbx_refresh`/`dbx_exp`/`dbx_verifier`)에 보관.
  access_token 만료 60초 전 `refreshToken()`으로 자동 갱신
- 저장 경로: `DATA_DIR = "/07_Apps/거래처관리(ERP)"` 아래 테이블별 JSON 파일 (v1.47~)
  ```
  /07_Apps/거래처관리(ERP)/companies.json  items.json    quotes.json
                            payments.json   stock_moves.json  settings.json
  ```
  읽기는 `readTable()`이 새 위치 → 옛 위치(`DATA_DIR_LEGACY = "/erp"`) 순으로 시도한다.
  폴더를 아직 안 옮겼거나 옛 App key로 접속해도 데이터가 보이게 하기 위한 폴백이고, **쓰기는 항상 새 위치**로 간다.
  한글 폴더명이라 `Dropbox-API-Arg` 헤더는 반드시 `dbxArg()`로 ASCII 이스케이프할 것 (헤더는 ASCII만 허용).
- `Table.load/loadObj/save` → `db` 전역 객체가 메모리 캐시.
  **저장 버튼 없음 = 의도적 설계** — 변경 시 해당 테이블만 `saveTable(name, data)`로 즉시 업로드
- `loadAll()`은 6개 테이블을 `Promise.all`로 병렬 로드 (상단바 새로고침 버튼)

## 데이터 모델
```js
db = { companies:[], items:[], quotes:[], payments:[], stock_moves:[], settings:{} }

Company {
  id, name, biz_no, type:"매출"|"매입", contact, phone, email, address,
  memo,          // 내부 메모 (출력물에 안 나감)
  quote_memo     // 견적서 기본 비고 — 이 거래처 선택 시 견적 비고에 자동 입력 (v1.26)
}

Item {
  id, code, name, type:"단품"|"세트"|"작업", spec, unit:"EA",   // code = 품목코드 (선택, 중복 불가)
  buy_price, sell_price,
  colors:["BK","WH"],                              // 색상 옵션 (별개 축)
  spec,                                            // 고정 규격 — variants가 있으면 쓰이지 않음
  variants:[{spec, buy_price, sell_price}],        // 사이즈 옵션 (단가가 다를 수 있는 축)
  components:[{item_id, color, spec, qty}],        // 세트 구성품 (type==="세트")
  // type==="작업"은 견적·매출에는 포함하지만 재고 현황·자동 출고에서는 제외
  memo
}

Quote {
  id, no:"Q-20260702-1",     // nextQuoteNo(date) — Q-YYYYMMDD-N 자동 채번
  date, company_id,
  status:"작성중"|"발송"|"수주"|"납품"|"취소",
  sent_at,        // 발송일 — '발송'으로 처음 바뀐 날
  delivered_at,   // 납품일 — 비어 있으면 미납품. **매출·미수금의 기준** (v1.37)
  tax_at,         // 세금계산서 발행일 — 비어 있으면 미발행 (v1.41)
  valid:"견적일로부터 1주일",
  lines:[{item_id, name, color, spec, unit, qty, price}],
  memo
}
// 견적번호는 nextQuoteNo()가 그날 최대 일련번호 +1로 채번 (개수 기반 아님 — 삭제해도 안 겹침)
// item_id === "__free__" → 품명 직접 입력 행 (품목 미등록 항목)

Payment {
  id, date, company_id, kind:"수금"|"지급", method, amount, memo, created_at,
  quote_id   // 수금을 특정 견적 건에 연결 (선택). 없으면 거래처 단위 상계만 됨 (v1.42)
}
StockMove {
  id, date, item_id, color, spec, kind:"입고"|"출고", qty, memo, created_at,
  quote_id   // 이 기록을 만든 견적 id (수동 입출고는 null) — 재고 정합의 핵심 (v1.27)
}
Settings  { name, ceo, biz_no, phone, email, bank, address }   // 견적서 공급자 란
```

## 화면 구조
탭(아이콘 레일) — `switchView(v)` → `renderers[v]()` 호출, `.view` 섹션 토글.
셸(v1.104~): **왼쪽 아이콘 레일(`.rail-gutter`>`.sidebar`, 호버 시 라벨 펼침) + 큰 카드 하나(`.main`)**.
상단바·하단 탭바 없음. 거래처·품목·견적은 큰 카드 안에서 세로 구분선 하나로 좌/우 분할.
모바일은 좌상단 햄버거(`#menuBtn` → `toggleNav`)로 레일을 드로어로 연다.

| 뷰 | 렌더러 | 역할 |
|----|--------|------|
| companies | `renderCoList` / `renderCoDetail` | 거래처 (목록 + 상세 폼) |
| items | `renderItList` / `renderItDetail` | 품목 (색상·규격/옵션·세트 구성·작업 품목) |
| stock | `renderStock` | 재고 현황 + 입출고 기록 |
| quotes | `renderQtList` / `renderQtDetail` | 견적서 작성·인쇄·이미지·공유 |
| payments | `renderPay` | 수금/지급 (월별) |
| dash | `renderDash` | 대시보드 (매출·마진·수금·지급·미수금·회신대기 + 월별 매출·수금 차트) |
| sales | `renderSales` | 매출 집계 (기간·거래처별 품목 집계 + CSV) |
| ar | `renderAr` | 미수금 — 거래처별 / 건별 두 가지 보기 (`#arView`) |
| settings | `renderSettings` | 공급자 정보 + 전체 데이터 백업/복원 |

- 거래처·품목·견적서는 좌(목록)·우(상세) 2단 `.cols` 그리드.
  모바일에서는 1열로 접히므로 목록 항목 선택 시 `scrollToDetail(formId)`로 상세까지 자동 스크롤
- 견적 저장 시 `syncStockForQuote(q)`가 재고를 목표 상태로 맞춘다 (아래 "재고 정합 설계" 참조)

## 견적서 출력 — 3가지 경로 × 2가지 양식
같은 견적서를 세 경로로 내보내고, 각 경로는 **견적서 / 거래명세서** 두 양식을 지원.
`DOC_TYPES`가 양식별 차이(제목·eyebrow·번호/일자 라벨·금액 라벨·유효기간 표시·푸터 문구)를
한곳에 모아두고, 세 경로가 모두 `docOf(docType)`으로 같은 값을 읽는다.
견적서 폼의 `#fq_doc` 선택값은 `qtDocType`(렌더 간 유지)에 보관되며 인쇄·이미지·공유가 공유한다.
**레이아웃이 서로 어긋나지 않게 함께 확인할 것**

1. **인쇄(PDF)** — `printQuote(q, docType)` → `#printArea`에 HTML 주입 후 `window.print()`
2. **미리보기(모바일)** — `openPrintPreview(q, docType)`. 모바일(≤820px)에서는 `window.print()`가
   막힌 인앱 브라우저가 많아 화면 내 오버레이를 먼저 띄움. 툴바: 닫기·이미지·공유·인쇄·PDF
3. **이미지(PNG)** — `drawQuoteCanvas(q, docType)`가 Canvas에 2배 해상도로 직접 그림.
   `saveQuoteImage(q, docType)`=파일 저장, `shareQuote(q, docType)`=공유

### 인쇄 CSS 핵심 (`@media print`)
- `@page{margin:16mm 14mm}`, `.p-wrap{min-height:250mm}` — 기타·안내문을 하단 고정(`.p-bottom{margin-top:auto}`)
  하면서 **A4 한 장을 넘지 않는** 값. 이 높이를 올리면 2페이지로 쪼개짐 (v1.12에서 263mm→250mm)
- `.pv-scale .p-wrap{padding:0!important; width:688px!important}` —
  미리보기용 여백을 인쇄에서 제거하고, 폭을 데스크탑 인쇄 폭(A4 182mm≈688px)으로 고정.
  이게 없으면 iOS Safari가 모바일 화면 폭(390px) 기준으로 렌더해 글자가 확대됨 (v1.13)
- `<meta name="format-detection" content="telephone=no">` +
  `#printArea a{color:inherit;text-decoration:none}` — iOS가 전화번호·계좌번호를 링크로
  바꿔 밑줄이 생기는 것 차단

### 캔버스 이미지 (`drawQuoteCanvas`)
- A4 96dpi 기준 `PW=794`, `MX=53`, `CW=688`, 2배 스케일(`SC=2`)
- 컬럼: 품명(가변 202) | 색상 46 | 규격 112 | 수량 58 | 단가 88 | 공급가액 100 | 세액 82 — 인쇄 `colgroup`과 동일.
  **폭은 실측 기준**("WH" 22px, "5000X900mm" 89px, 품명 최장 143px) — 줄이면 줄바꿈이 생긴다
- 열별 정렬은 `ALIGN` 배열 한곳에서 정의하고 `cell()` 헬퍼로 그린다.
  인쇄 표의 `.ctr`/`.num` 클래스와 **같은 규칙을 유지할 것** (품명·색상 왼쪽 / 규격·수량 가운데 / 금액 오른쪽)
- **측정 패스 → 드로잉 패스** 2단 구조. 품명·주소·메모를 `qimgWrap()`으로 줄바꿈 측정한 뒤
  전체 높이를 계산하고, 하단 블록(합계·기타·푸터)은 페이지 하단에 고정 배치
- 품목이 많으면 이미지 높이가 1123px(A4)에서 자동으로 늘어남

### 공유 (`shareQuote`)
환경별 자동 fallback — 3단계
1. `navigator.canShare({files})` → 공유 시트 (iOS·안드로이드·macOS Safari·Windows Chrome).
   **`navigator.share()`에는 `files`만 넘긴다** — `text`/`title`을 함께 넘기면 카카오톡 등에서
   이미지와 별개로 텍스트 메시지가 하나 더 전송되므로 의도적으로 제외 (v1.25)
2. `navigator.clipboard.write(ClipboardItem)` → 클립보드 복사 (그 외 데스크탑, 붙여넣기로 전송)
3. `downloadBlob()` → PNG 파일 저장

## 모바일 대응
- 분기: **≤820px** (사이드바 드로어 전환), ≤980px (2단→1단 그리드), ≤560px (grid2/grid3→1열)
- 레일(`.sidebar`)은 `position:fixed` + `transform:translateX(-105%)` 드로어.
  좌상단 햄버거(`.m-burger#menuBtn`) → `toggleNav()`, 백드롭(`#navBackdrop`) 클릭 시 닫힘.
  `switchView()`가 항상 `toggleNav(false)` + `scrollTo(0,0)` 실행.
  ⚠️ 데스크탑 접힘 규칙이 `.sidebar:not(:hover)`(특이도 높음)라, 드로어에서 라벨을 다시 보이려면
  모바일 규칙도 **같은 `:not(:hover)`로** 덮어야 한다(안 그러면 드로어에 아이콘만 나옴 — v1.104).
- **모바일 CSS는 컴포넌트 규칙보다 뒤(인쇄 CSS 직전)에 위치** — 앞에 두면 나중에 오는
  `.qline`·`.stock-add` 등에 덮여서 무효가 됨 (v1.10에서 실제로 겪은 문제)
- 넓은 표는 `overflow-x:auto` + `min-width`로 가로 스크롤:
  `.qline` 560px, `.cline` 440px, `.vline` 400px, `#slBody .tbl` 860px
- iOS 입력 확대 방지: viewport에 `maximum-scale=1.0, user-scalable=no`

## 디자인 원칙 (dutch-pay·receipt-db와 통일)
- 배경 `--paper:#F2F4F7`, 카드 `#FFFFFF`, 선 `--line:#E5E9EF`
- 강조 `--primary:#0A84FF`, 위험 `--danger:#FF3B30`, 성공 `--ok:#34C759`, 경고 `--warn-c:#FF9F0A`
- 모서리 `--r:16px / --r-sm:10px`, 폰트는 시스템 스택 (외부 폰트·CDN 없음 — 오프라인 동작)
- 아이콘은 인라인 SVG(lucide 계열)만. 앱 아이콘도 같은 육각형 로고 + `#0A84FF` 배경
- 숫자는 `font-variant-numeric:tabular-nums` + `fmt()`/`won()`로 천단위 쉼표

## 백업 / 오프라인
- **백업 JSON** — `exportBackup()`이 `{app:"erp", appVersion, format:1, exportedAt, settings, ...5개 테이블}`
  형태로 저장. `importBackup()`은 `app!=="erp"`이거나 테이블이 배열이 아니면 **아무것도 반영하지 않고 중단**
  (부분 복원으로 데이터가 섞이는 것 방지) → 확인 후 전체 덮어쓰기 + Dropbox 재업로드
- **서비스워커(`sw.js`)** — 앱 껍데기만 캐시(`erp-shell-v1`). 데이터는 Dropbox API라 캐시하지 않음
  - HTML은 **network-first**: 새 배포를 즉시 받고, 오프라인일 때만 캐시 사본을 씀.
    사용자가 버전 칩으로 배포를 확인하는 습관과 충돌하지 않도록 한 의도적 선택 —
    cache-first로 바꾸면 배포해도 옛 버전이 계속 보인다
  - 아이콘·매니페스트는 cache-first. **파일을 바꾸면 `CACHE` 상수(`erp-shell-v1`)를 올릴 것**
  - `manifest.webmanifest`의 `display`는 `browser` 유지 — standalone으로 바꾸면 iOS에서
    Safari 인쇄 경로가 막혀 견적서 인쇄가 어려워진다

## 재고 정합 설계 (v1.27~) — 건드리기 전에 반드시 읽을 것
견적과 재고는 **"목표 상태로 맞추는(reconcile)" 방식**으로 연결돼 있다. 차감을 쌓는 방식이 아니다.

- `quoteStockNeeds(q)` — 이 견적이 재고에서 빼야 할 목표 수량.
  세트는 구성품으로 분해하고 `작업` 품목은 제외한다.
- `quoteStockDone(q.id)` — `stock_moves.quote_id`로 추적한, 이미 반영된 수량
- `stockDeltaForQuote(q)` — 목표 − 현재 = 기록할 입출고. 차이가 0이면 아무것도 안 만든다
- `syncStockForQuote(q)` — 위 차이를 확인받고 반영. **멱등** — 여러 번 실행해도 결과가 같다

이 구조 덕분에 수주↔취소 반복, 수량 증감, 재저장, 견적 삭제가 모두 자동으로 맞는다.
**차감 로직을 다시 "상태가 바뀔 때만 빼기"로 되돌리지 말 것** — v1.26 이전에 그렇게 돼 있어서
이중 차감·미복구 버그가 있었다 (v1.27에서 수정).

- `stock_moves.quote_id`: 견적이 만든 기록이면 견적 id, 수동 입출고면 `null`
- `migrateStockQuoteLinks()` — v1.26 이전 기록에는 `quote_id`가 없어서 메모의 견적번호로
  역추적해 채운다. `loadAll()`에서 실행. **이게 없으면 기존 수주 건 재저장 시 이중 차감된다**

## 매출·미수금 기준 (v1.37~) — 중요
**매출과 미수금은 "수주"가 아니라 "납품"이 기준이다.** `delivered_at`이 있으면 납품된 것으로 본다
(`isDelivered(q)`). 수주만 하고 아직 납품 전인 건은 청구할 수 없으므로 미수금이 아니라
**미납품 잔액**으로 따로 표시한다.

- 미수금 = Σ(납품액) − Σ(수금)  ·  미납품 = Σ(수주액) − Σ(납품액)
- 대시보드 이달 매출도 `delivered_at` 기준 (`monthSales`)
- 재고는 `QT_COMMITTED=["수주","납품"]` — 수주하면 제작에 들어가므로 납품까지 계속 나가 있다
- 상태를 '납품'으로 바꾸거나 납품일을 넣으면 서로 자동으로 맞춰진다

### 스키마 마이그레이션 (`runMigrations`, `settings.schema`)
`SCHEMA=3`. **반드시 버전을 기록하고 1회만 실행해야 한다** — 매번 돌면 새 수주 건까지
납품 처리돼 버린다. v3는 기존 '수주' 건에 `delivered_at=q.date`를 채워
기준 변경 전후로 미수금 수치가 달라지지 않게 한다.

## 숫자 입력 규칙 (v1.38~)
- 수량·단가 입력은 `type="text" inputmode="numeric"`. **`type="number"`를 쓰지 말 것** —
  스피너가 자리를 예약해 가운데·오른쪽 정렬을 밀어낸다 (v1.38에서 겪은 문제)
- 입력칸에도 천단위 쉼표를 표시한다. 포커스 시 쉼표를 걷고 `select()`,
  블러 시 다시 쉼표. 값 읽기는 항상 `parseNum()`
- `select()`는 **동기로 호출**할 것 — `setTimeout`으로 미루면 비동기 포커스가
  Tab 이동을 삼킨다 (사이즈 패널에서 실제로 발생)
- 헤더 라벨을 오른쪽 정렬할 땐 입력칸의 테두리+여백(10px)만큼 `padding-right`를 줘야
  숫자와 줄이 맞는다

## 규격 / 색상 / 사이즈의 관계 (v1.43~)
견적 행은 `color`와 `spec` 두 축만 가진다.
- **색상**(`Item.colors` → `line.color`) — 완전히 별개 축. 출력물에서도 별도 열
- **규격**(`line.spec`) — 사이즈 옵션이 있으면 **선택한 사이즈**(L·2XL·30inch)가,
  없으면 **품목의 고정 규격**(`Item.spec`, 예: 80X40cm)이 들어간다
- 즉 **사이즈와 규격은 같은 필드를 공유**한다. 한 품목이 둘을 동시에 가질 수 없어
  "타월 110g"처럼 규격 일부를 품명에 넣는 우회가 쓰이고 있다.
  둘 다 필요해지면 `line.size`를 새로 만들고 출력 열을 하나 더 늘려야 한다 (아직 미구현)

## BOM(소요량) — 세트 전개 규칙 (v1.46~)
`세트` 품목이 곧 BOM이다. `expandBom(item_id,color,spec,qty,out,path)`이 **원자재·작업까지 재귀로**
펼친다 — 세트 안에 세트가 들어가도 끝까지 내려가고, 순환 참조(A⊃B, B⊃A)는 `path`로 차단한다.

- `bomOfSet(i)` — 세트 1개당 구성 소요 맵
- `setCompSums(i)` — 전개된 구성의 매입·매출 합 (중첩 세트 가격도 정확)
- `quoteStockNeeds(q)` — 견적 소요량. **세트를 여기서 직접 펼치지 말고 `expandBom`을 쓸 것**
  단, 최종 재고 차감은 `tracksStock()`이 참인 품목만 반영한다.
- `quoteShortage(q)` — 소요량 대비 부족 자재. 이 견적이 이미 뺀 양(`quoteStockDone`)을
  되돌려 계산하므로 수주 후에도 중복으로 잡히지 않는다
- **`quoteStockDone(qid)`는 `qid`가 없으면 빈 값을 반환해야 한다** — 저장 전 견적(id=null)에서
  수동 입출고(`quote_id=null`)까지 매칭되어 보유 재고가 0으로 보이던 버그가 있었다 (v1.46)

**생산입고(제조 후 완제품 입고)는 일부러 넣지 않았다** — 무지 제품을 사서 자수만 하는 업태라
완제품 재고를 따로 잡을 일이 없다. 필요해지면 `stock_moves`에 `kind:"생산"`을 더하는 방식이 맞다.

## 마진 계산 규칙 (v1.29~)
`itemBuy(i,spec)`가 매입단가를 찾고(옵션별 단가 우선 → 기본 단가 → 세트는 구성품 합계),
`quoteMargin(q)`가 견적 마진을 낸다.
**매입단가를 모르는 항목은 0원으로 치지 말고 계산에서 제외하고 몇 건인지 표시할 것** —
0으로 처리하면 마진이 실제보다 부풀려진다. 견적 마진은 화면에만 표시하고
**인쇄·이미지 출력물에는 절대 넣지 않는다** (고객에게 나가는 문서다).

## 남은 결함 (2단계에서 처리)
- **미수금이 "수주 = 매출" 기준** — 아직 납품 안 한 건도 미수금으로 잡혀 부풀려진다.
  납품(출고) 문서를 분리해야 정확해짐. `ROADMAP.md` 2단계 참조

## 알려진 함정 (작업 시 주의)
- **인쇄·미리보기·캔버스 3중 레이아웃** — 하나만 고치면 나머지가 어긋남. 견적서 출력을
  건드릴 때는 셋 다 확인 (PDF 페이지 수, 모바일 미리보기, PNG)
- **표 열 폭 고정 시 min-width 확인** — `table-layout:fixed` + `colgroup`으로 열을 고정하면
  모바일 `min-width`가 고정 열 합계보다 충분히 커야 함. v1.17에서 min-width 660px이
  고정 열 합 640px과 거의 같아 품명 열이 20px로 짜부라져 글자가 겹쳤음
- **여러 테이블의 열 정렬** — 매출 집계처럼 카드가 반복되는 화면은 열 폭을 명시하지 않으면
  브라우저가 카드마다 따로 계산해 열 위치가 제각각이 됨 (v1.15)
- `escapeHtml`/`escapeAttr`을 거치지 않은 사용자 입력을 innerHTML에 넣지 말 것
- 렌더 함수는 innerHTML로 전체를 다시 그리므로, 이벤트는 **매 렌더마다 재바인딩** 필요
  (`bindQtLines`, `renderCoDetail` 하단 `onclick` 할당 패턴)
- `qtEditing`/`itEditing`은 편집 중 사본. 새로 만들기(`__new__`) 시 반드시 초기화
  (v1.8에서 이전 편집 내용이 남는 버그)

## 검증 방법
로컬에 Playwright(`playwright-core` + `/opt/pw-browsers/chromium`)로 스크린샷·PDF를 뽑아 확인.
Dropbox 로그인은 우회하고 `db`에 직접 샘플 데이터를 주입:
```js
document.getElementById('loginView').classList.add('hidden');
document.getElementById('appView').classList.remove('hidden');
db.companies.push({id:'c1', name:'메디랩코리아(주)', type:'매출'});
db.quotes.push({ /* ... */ });
switchView('quotes');
```
- 인쇄 검증은 `page.pdf({format:'A4'})` → 페이지 수 확인 (`/Type /Page` 카운트)
- 모바일은 `viewport:{width:390,height:844}, isMobile:true, hasTouch:true`

## Git 작업 방법
```bash
git add index.html          # 변경된 파일만 명시적으로 추가 (git add -A 금지)
git commit -m "vX.Y: 요약"
git push -u origin main     # 라이브 반영 — 사용자 승인 후에만
```
- 개발은 작업 브랜치에서, **main 병합·푸시는 사용자가 "병합해줘"라고 할 때만** 실행
- GitHub Pages는 main에서 배포 — 병합 전에는 라이브에 반영되지 않음

## Changelog
- `v1.118` — **견적서 기본정보 Form 밀도 정리(디자인 시스템 v1.0, 기본정보만)**. 공통 `.field/input/select`를 건드리지 않도록 기본정보 `.qt-sec`에 `qt-basic` 클래스, 상태·유효기간 `.grid2`에 `pair` 클래스만 추가(마크업 최소 변경)하고 전부 `#view-quotes .qt-basic` 스코프로 CSS. ① **세로 길이 축소** — 필드 간격 16→12px(모바일 10px), 라벨~입력 간격 6→5px(모바일)로 조밀하게. ② **상태·유효기간 2열 유지** — 짧은 필드라 모바일(≤560)에서 1열로 접히던 것을 `pair`만 2열 유지해 한 줄 절약(거래처·견적일자·세금계산서는 풀와이드 유지). 라벨 13.5/600·보조색. 입력 높이·radius·기능·필드·순서·날짜/거래처/상태 Select·저장·계산 무변경. 상단 Summary·품목(v1.117)·비고·버튼·하단 탭·다른 화면 무변경. sw.js CACHE v23→v24.
- `v1.117` — **견적 품목 입력 UI 정리(디자인 시스템 v1.0, 품목 화면만)**. 전부 `#view-quotes` 스코프, 마크업·계산·기능 무변경. ① **모바일 스택 레이아웃** — 데스크탑 표(`.qline` grid)를 가로 스크롤(`min-width:620px;overflow-x:auto`)로 축소하던 것을 `@media(max-width:820px)`에서 `grid-template-areas`로 한 품목 **3줄 재배치**: 1행 `품명 [삭제]` · 2행 `규격/옵션` · 3행 `수량 · 단가 · 금액`. 가로 overflow 제거(`overflow-x:visible`), 모바일 열 머리글(`.qline.head`) 숨김, 입력칸 40px·15px. ② **금액=자동계산 값** — 모바일에서 `.amt`(span, 테두리 없음)를 우측정렬·15px/700로 강조해 수량·단가 입력칸과 구분(수정 가능한 것처럼 안 보이게). 수량·단가 우측정렬. ③ **합계 라벨** 16/600으로 소폭 보정(값 20/780·행 14px·구분선은 그대로). 품목 추가(`.btn-line-add` 점선 아웃라인=Secondary)·삭제(`.rm` 회색→hover 위험색)는 이미 스펙 부합이라 무변경. 데스크탑 표·상단 Summary·기본정보 폼(공통 `.field`)·하단 탭·다른 화면·계산 무변경. sw.js CACHE v22→v23.
- `v1.116` — **견적서 상세 Summary 세로 정렬 미세 조정(견적 상세만, 1줄)**. 요청: 견적번호(Q-…)와 금액(742,500원) 텍스트의 **세로 시작 위치(Y축)만** 정확히 정렬(폰트 크기·위계는 그대로 유지). 금액 라벨 `.qs-amt-k`의 line-box를 좌측 라벨과 동일하게(`line-height:18px`) + 라벨~값 간격을 좌측(`.qs-row` gap 3px)과 동일하게(`margin-bottom:7px→3px`) 맞춰 값 시작 top을 정렬(헤드리스 실측 165=165, 차이 0.00). 폰트 크기(번호 15·금액 26)·색·라벨 텍스트·거래처·작성일·작성중 배지·폼·하단 탭·다른 화면 무변경. `#view-quotes` 스코프. sw.js CACHE v21→v22.
- `v1.115` — **견적서 상세 상단 Summary UI 정리(디자인 시스템 v1.0, 견적 상세만)**. `renderQtDetail`의 히어로만 2영역 Summary로 재구성(전부 `#view-quotes` 스코프). 좌: `견적서 번호`(작고 차분, mono 15px)+거래처+작성일(보조정보 12/15px), 우: **`금액 (부가세 별도)` 라벨 + 금액(33px/모바일 26px, 가장 강함)**. 두 블록 `align-items:flex-start`로 견적번호·금액의 **세로 시작 위치만** 맞춤(폰트 크기는 다름). **금액=공급가액(t.supply, 부가세 별도)** — 히어로 표시와 타이핑 실시간 갱신(`fq_heroTotal`) 모두 total→supply로 통일(계산 함수 `quoteTotals` 무변경). **총 품목 수량·`합계`(총액)·장식 아이콘 제거**, 상태는 번호 옆 작은 배지로 최소화(상태 변경은 폼의 `fq_status` select로 그대로). 공통 `.qt-hero`(패딩·하단 구분선) 재사용, 새 카드 없음. 기본정보 Form(공통 `.field`)·버튼(저장=Primary·공유 등 ghost)·탭·하단 탭·다른 화면·계산·저장 무변경. sw.js CACHE v20→v21. main 병합은 승인 후.
- `v1.114` — **대시보드 월별 차트 카드 높이 축소(모바일, 대시보드만)**. 사용자 요청 2건 중: ① **제목 아래 선** — v1.113에서 이미 대시보드 제목줄 `border-bottom`을 제거했으므로 추가 변경 없음(라이브 v1.112엔 남아 있어 배포 시 사라짐). Hero~KPI 구분선(`.stats` border-top)은 요청대로 유지. ② **차트 높이** — 모바일 `#view-dash .chart` 높이 150→128px(약 15%↓)·내부 상하 패딩 축소(`10px 14px 8px`)로 세로 빈 공간을 줄여 밀도 향상. 막대는 % 높이라 비율·데이터·범례·색상·계산 무변경, 월 라벨·막대 잘림 없음. KPI·최근활동·하단탭·다른 화면 무변경. sw.js CACHE v19→v20.
- `v1.113` — **대시보드 상단 Hero 맥락·위계 보완(대시보드만, 모바일)**. 요청: 제목 `대시보드`와 Hero(이달 매출 라벨·금액·부연)가 하나의 상단 정보 블록으로 읽히게. 모바일에서 제목줄과 Hero 사이의 구분선(공통 sticky 규칙의 `border-bottom:var(--line-2)`)을 대시보드에만 제거하고 제목줄 높이·간격을 축소(`height:auto;padding:12px 14px 2px`, Hero `margin:6px 0 2px`)해 `대시보드 → 이달 매출·납품 기준 (09월) → 금액 → 납품·미수금` 순서로 묶음. Hero~KPI 6개 사이 구분선/간격은 `.stats`의 border-top으로 그대로 유지. 위계: 금액(30px)>대시보드(19px)>라벨(13px)>부연(13px). 금액 크기·아이콘·카드·그림자·그라데이션 추가 없음. KPI·차트·최근활동·하단 탭·입금 등록·새 견적·계산·다른 화면 무변경. 전부 `#view-dash` 스코프. 데스크탑은 원래 제목줄 구분선이 없어 영향 없음. sw.js CACHE v18→v19.
- `v1.112` — **대시보드 Hero 겹침 수정(iOS)** — 실기기 제보: 모바일 대시보드에서 이달 매출 금액(746,350원)이 상단으로 밀려 잘리고 제목 위로 올라와 보임. 원인은 iOS Safari가 `overflow:auto` 스크롤 컨테이너(그리고 그 컨테이너가 flex 자식일 때) 안의 `position:sticky` 헤더를 흐름에서 빼내 바로 아래 히어로 위로 겹쳐 그리는 버그(헤드리스 Chromium에선 정상이라 안 잡힘). **대시보드 제목줄만** `position:static`으로 고정 해제(`@media(max-width:820px){.shell .main>#view-dash>.page-head}`, id 특이도로 공통 sticky 규칙을 덮음)해 정상 흐름 복원 — 제목은 그대로 상단, 스크롤 시 함께 올라감. 히어로 상단 여백 14px. 다른 화면의 sticky 헤더·데스크탑(원래 sticky 아님)·KPI·차트·최근활동·하단 탭은 무변경. sw.js CACHE v17→v18. ⚠️ 다른 화면들도 같은 sticky 패턴이라 동일 잠재 이슈 가능 — 공통 Navigation/Header polish 단계에서 함께 점검 예정.
- `v1.111` — **대시보드 UI 다듬기(디자인 시스템 v1.0) — 대시보드 화면만**. 정보·계산·차트 데이터·다른 화면 무변경. 모든 CSS는 **`#view-dash` 스코프**(공통 `.scr-hero`/`.stats`/`.stat`/`.card-head`/`.recent`/`.pill`은 미수금·수금·거래처 화면과 공유하므로 스코프로 격리, 렌더러 마크업 변경도 `renderDash` 한정). ① **Hero KPI** — 이달 매출을 가장 강하게(30→32px, 모바일 30px), 라벨·부연은 차분하게(색 `--text-muted/secondary`). ② **KPI 위계** — `renderDash`의 6개 `.stat`에 수식 클래스 추가: 이달 수금·미수금 합계=`stat-key`(값 20px 강조), 회신 대기·계산서 미발행=`stat-soft`(값 15px 조용, 의미색 유지). 마진·지급은 기본(17px). 큰 카드 6개로 만들지 않고 기존 인라인 `.stats` 유지. ③ **CTA** — 기존대로 `+ 새 견적`=Primary, `입금 등록`=Secondary(outline). 변경 없음(이미 스펙 부합). ④ **차트** — 모바일 세로 빈 공간 축소(`#view-dash .chart` height 150px+패딩 축소), 섹션 제목 16/700, 범례 12px. 차트 데이터·계산 무변경. ⑤ **최근 활동** — 마크업을 `내용>금액>상태>날짜` 위계로 재구성(`.t` flex + `.ra` 금액 + 작은 `.pill` + `.d` 날짜), 가벼운 행 구분선(`--line-2`), 배지 축소(11px)로 행 높이 안정. 같은 데이터·정렬·8건 유지. sw.js CACHE v16→v17. main 병합은 승인 후.
- `v1.110` — **THE EDIT UI Design System v1.0 — 1차(토큰/타이포 + 모바일 하단 탭)**. 화면별 UI(견적 요약·폼·대시보드·표·카드)는 **미변경**. ① **디자인 토큰** — `<style>` 끝에 late-override `:root` 블록 추가: canonical 토큰 정의(`--text-primary #171717`·`--text-secondary #666`·`--text-muted #9A9A9A`·`--bg-page`·`--bg-card`·`--border-light/default/strong`·`--primary #4355E8`·`--radius-card/control`·`--fs-*`). 기존 별칭을 canonical에 연결(색상만): `--ink→#171717`, `--ink-3 #B0B0B8→#9A9A9A`, `--line #EBEBED→#E2E2E2`(그 외 동일). **반경(--r/--r-sm)은 이번에 연결 안 함**(카드 기하 변경 없음). body만 `font-size:var(--fs-body)`(15px 동일)로 연결. **CSS 삭제 없음**(dead CSS 정리는 최종 단계). ② **모바일 하단 탭** — `.bottom-nav`(신규 클래스, 옛 `.mobile-nav`의 `display:none!important`·알약 스타일과 분리) 5개 균등 탭: 홈(dash)·견적서·수금/지급·재고·더보기. 버튼은 `.mobile-nav-item[data-view]`라 **기존 `switchView` 하이라이트·바인딩 재사용**(새 라우터 없음). 옛 `.mobile-nav-item.on` 알약 배경/밑줄은 `.bottom-nav>button.on`으로 무력화(선택=Primary Blue 글자만). **더보기**=기존 드로어 재사용(`#moreNavBtn`→`toggleNav(true)`), 2차 메뉴(거래처·품목·자재·매출·미수금·공급자·설정)+레일 하단 액션(명함·사업자등록증·새로고침·로그아웃) 그대로. 2차 메뉴 화면일 때 더보기 강조(switchView에 null-guard 1줄 추가). ③ **햄버거** — 모바일에서 `.m-burger{display:none!important}`(하단 탭과 동시표시 금지), 사라진 만큼 page-head `padding-left 60→14px` 원복. ④ **Safe Area** — `.bottom-nav`에 `padding-bottom:env(safe-area-inset-bottom)`, 모든 뷰에 `padding-bottom:calc(56px+safe-area+16px)`로 본문 가림 방지. **데스크탑(아이콘 레일) 무변경** — `.bottom-nav`는 `@media(max-width:820px)`에서만 표시. 계산·저장·집계·출력물·History/Back 무변경. sw.js CACHE v15→v16. main 병합은 사용자 승인 후.
- `v1.109` — **수금/지급·업체 제공 자재 카드 스크롤 수정**(사용자 제보: "수금 지급 탭에서의 카드안은 스크롤이 안되고"). 내용형 화면(`#view-payments`·`#view-materials` 등)이 `.shell .main>.view`의 `display:flex;flex-direction:column`을 상속해, 안쪽 카드가 flex로 줄어들며(`flex-shrink`) `overflow:hidden`에 잘려 저장/추가 버튼이 화면 밖으로 사라졌다. 내용형 화면을 `display:block;overflow:auto`(자연 흐름 + 자체 스크롤)로 되돌리고, 마스터-디테일(거래처·품목·견적)만 명시적으로 `display:flex;flex-direction:column`을 유지. `.shell .main>.view:not(.hidden)`의 공통 규칙에서 `display:flex`를 제거해 뷰 유형별로 지정. 계산·저장·집계·출력물 무변경. sw.js CACHE v14→v15. ※ 디자인 시스템 v1.0 적용은 사용자 승인 후 별도 진행.
- `v1.108` — **모바일 고정 헤더 통일 + 대시보드 겹침 수정**(사용자 피드백). ① **대시보드 버튼-내용 겹침** — `.page-head`가 `flex-wrap`인데 버튼(head-actions)이 안 맞아 2줄로 넘치며 히어로와 겹침(v1.107의 `margin:0`이 간격도 제거). 모바일 헤더를 **고정 높이 한 줄(`--m-head-h:58px`, nowrap, overflow:hidden)** 로 재설계 — 제목 축소·버튼 인라인·`#view-dash .head-actions` 인라인 복원. ② **견적서 검색창까지 고정**(요청) — 리스트 화면(거래처·품목·견적)의 `.list-head`(검색창)를 `position:sticky;top:var(--m-head-h)`로 헤더 바로 아래 고정, 리스트만 스크롤. ③ **햄버거-제목 겹침** — page-head `padding-left:60px`(마스터-디테일은 데스크탑 id 규칙이 이겨서 모바일에서 같은 id 특이도로 재지정). ④ **매출집계·미수금** — 필터 컨트롤이 많아 한 줄이 안 되므로 컨트롤을 `.page-head`에서 빼내 **본문 첫 줄 `.filter-bar`로 이동**(id 유지, 기능 무변경). 데스크탑에선 `.date-control` 래퍼가 풀와이드라 세로로 쌓이던 것도 폭 150px로 제한. sw.js CACHE v13→v14. 계산 테스트(quote-workflow) 통과.
- `v1.107` — **실기기(iOS) 3종 버그 수정**(사용자 제보). ① **레일 섹션 라벨 깨짐** — `.nav-sec`를 접힘 시 `height:0`+`overflow:hidden`로 숨기던 방식이 iOS에서 17px로 클리핑돼 글자가 잘림. 접힘=`display:none`, 펼침/드로어=`display:block`로 교체하고 base `.nav-sec`에 `overflow:visible;line-height:1.5` 적용. ② **모바일 상단 미고정** — 마스터-디테일/내용형 화면이 뷰 전체 스크롤이라 제목(page-head)이 스크롤하면 사라짐 → `@media(max-width:820px)`에서 `.main>.view>.page-head`를 `position:sticky;top:0;background:var(--card)`로 고정(햄버거는 이미 `.main` 기준 absolute라 유지). ③ **뒤로가기** — history 처리가 전혀 없어 OS 뒤로가기가 앱을 나가버림(하단 탭바 제거로 더 불편). `toggleNav`에 드로어 열림 시 `history.pushState` 1개를 쌓고, `popstate`에서 드로어가 열려 있으면 닫는다(⚠️ **popstate 안에서는 pushState 하지 않음** — dutch-pay v5.84 교훈, 안드로이드 이중전환 방지). 사용자가 백드롭으로 닫으면 `history.back()`로 쌓인 항목을 소진. `_navBackArmed`/`_inNavPop` 가드. sw.js CACHE v12→v13.
- `v1.106` — **명함 보내기·사업자등록증 보내기를 레일 하단에 직접 노출**(사용자 요청). v1.104에서 설정(공급자 정보) 헤더로 옮겼던 두 버튼을 `.rail-foot`의 `.rail-act`(동기화 점·계정 아래, 새로고침·로그아웃 위)로 이동. id(`bizCardBtn`/`bizCertBtn`)는 그대로라 init 바인딩 무변경. 접힘 시 아이콘만, 호버/드로어 시 라벨. 설정 헤더의 두 버튼은 제거(같은 id 중복 방지). 기능 무변경.
- `v1.105` — **셸 치수를 더치페이와 동일하게 맞춤**(사용자 요청). 전체 프레임 `.shell`을 **max-width 1484px + `margin:0 auto`(중앙정렬)**, 레일 폭 64→**76px**, 마스터-디테일 왼쪽 패널을 42%/440 → **360px 고정**, 오른쪽 패널은 나머지(1fr), 큰 카드 라운드 20→**22px**. 더치페이 `.desktop-app`(`grid-template-columns:76px 360px 1fr; max-width:1484px; border-radius:22px`)와 동일. 기능·집계 무변경.
- `v1.104` — **더치페이식 셸로 전면 개편(껍데기만, 기능·집계·출력물 무변경)**. ① **상단바·하단 탭바 제거 → 왼쪽 아이콘 레일 하나로 통합**(`.rail-gutter`>`.sidebar`). 레일은 접힘 64px(아이콘만), **마우스 올리면 216px로 펼쳐지며 라벨 표시**(내용 안 밀고 위에 뜨는 오버레이 + 닫힘 지연 0.28s). 로고(ERP+버전)는 레일 상단, 하단 `.rail-foot`에 **동기화 상태 점(●)**·계정·새로고침·로그아웃. 상단바에 있던 **명함 보내기·사업자등록증은 설정(공급자 정보) 화면 헤더**로 이동(같은 id 유지 → 기존 바인딩 그대로). 기존 nav-item/`switchView`/`toggleNav`/`menuBtn`/`navBackdrop` 재사용(라벨만 `<span>`으로 감쌈). ② **본문을 큰 카드 하나로**(`.shell>.main`) — 마스터-디테일(거래처·품목·견적)은 카드 2개+gap을 **큰 카드 안에서 세로 구분선 하나**로 병합(패널 각자 스크롤, 필터 틴트 밴드 제거). 대시보드·재고·수금·매출·미수금·자재는 흰 카드 위 콘텐츠로. v1.98의 `100vh-64px`·sticky 프레임을 flex로 대체(상단바 64px 사라진 만큼 재계산). ③ **모바일은 햄버거 드로어**(`.m-burger` → `toggleNav`, 좌측 라벨 메뉴 슬라이드 + 딤). ⚠️ 접힘 규칙이 `:not(:hover)`라 특이도가 높으니, 드로어에서 라벨 다시 보이려면 모바일 규칙도 **같은 `:not(:hover)`로** 덮어야 한다(안 그러면 드로어에 아이콘만 나옴). CSS는 `<style>` 끝의 `v1.104` 블록에 몰아둠(late override). `sw.js` CACHE `erp-shell-v9`→`v10`. 헤드리스 데스크탑(견적·거래처·품목·대시보드·재고·자재·설정)·모바일(견적·드로어·대시보드)·레일 호버 검증, JS 오류 0.
- `v1.103` — 좁은 폭(≤820px·≤430px)에서도 견적 목록 행 좌우 여백 확대 — v1.102가 데스크탑 기본만 30px로 바꿔 모바일/좁은 창은 그대로 10px였던 것 보정(≤820px 24px, ≤430px 18px). 상호명·금액이 검색창과 같은 좌우 여백으로 정렬.
- `v1.102` — 견적 목록 행 좌우 여백 15→30px(사용자 요청). 구분선 인셋은 10px 유지(글자보다 길게).
- `v1.101` — 견적 목록 여백·구분선 재조정(사용자 피드백). 행 좌우 패딩 20→15px로 줄여 글자가 덜 답답하게, 구분선 인셋 20→10px로 늘려(`.list-item{--div-inset:10px}`) 구분선이 글자보다 살짝 더 길게(더치페이식). 이전 v1.99의 20px가 과해 글자가 답답하고 구분선이 짧아 보이던 것 보정.
- `v1.100` — **할인(음수 단가) 행을 재고 계산에서 제외**(사용자 지적). `quoteStockNeeds`가 견적 행을 돌 때 `l.price<0`(할인)이면 건너뛰도록 추가 — 할인은 실물이 아니라 재고를 차감/필요로 하지 않는다(`isDeliveryLine`이 이미 음수가·작업을 납품 대상에서 뺀 것과 같은 기준). 이전엔 `legacy_stock` 견적이나 재고부족 경고 계산에서 할인 단품이 재고를 소요하는 것으로 잡히던 문제. 헤드리스로 할인 행이 재고 소요에서 빠지는 것 확인. 재고 정합(reconcile) 구조·집계 무변경.
- `v1.99` — 목록·구분선·카드 간격 다듬기(사용자 피드백 다수). ① **목록 hover/선택 박스 직각** — 모바일 breakpoint의 `.list-item{border-radius}`도 0으로(hover 회색 배경 둥근 모서리 제거). ② **왼쪽 패널 폭 확대** — 견적 `minmax(320,380)`→`minmax(360,440)`, 거래처·품목 `.cols` 340/320→380/360 등. ③ **목록 행 좌우 여분 확대 + 구분선 정렬** — `.list-item` 좌우 패딩 16→20px, `--div-inset`도 20px로 맞춰 구분선이 상호명 시작~금액 끝과 정확히 일치. ④ **입출고 내역 구분선을 글자에 정렬** — 구분선 인셋 16→2px(`.material-history{--div-inset:2px}`)로 기록 좌우 패딩에 맞춰 글자(날짜~수량)와 정렬, 바깥 여분은 카드 패딩이 담당(예전엔 16px라 글자 안쪽에 들어가 이질감). ⑤ **수금/납품 카드 회색 헤더~합계 간격 균일화** — `.quote-flow-body` 상단 패딩 0/15→12px, `.payment-totals` 상단 패딩 제거 → 회색 헤더~첫 행 간격이 행 간격(12px)과 동일. 집계·저장 로직 무변경.
- `v1.98` — **두-패널 화면(거래처·품목·견적)을 더치페이식 고정 프레임 + 좌/우 독립 스크롤로**(사용자 요청). 데스크탑 2단 구간(**≥981px**)에서 좌·우 카드를 각각 `position:sticky;top:0` + 화면 높이(`calc(100vh-214px)`)로 고정 → 페이지(프레임)는 고정되고 긴 목록/긴 상세는 **각 카드 안에서만** 스크롤. 우측 상세 카드가 자체 스크롤러가 되고(`overflow-y:auto`, `.detail{min-height:100%;padding-bottom:0}`), 저장바는 우측 카드 바닥에 `sticky bottom:0`로 고정(더 이상 `.main` 패딩 보정 불필요). ⚠️ **981px+에서만** 적용 — 거래처·품목은 ≤980px에서 1단으로 접혀 고정 높이면 세로로 겹쳐 깨지므로, 실제 2단 구간에서만 켠다. 단일 컬럼 화면(대시보드·매출 등)·모바일(≤820px)은 무영향. 헤드리스로 1300/1000px 프레임 고정(page slack 0)·좌우 독립 스크롤·저장바 하단 고정, 900px·모바일 정상 폴백 확인.
- `v1.97` — 매출 집계 표 2종(사용자 피드백). ① **표 구분선 좌우 여백** — `.tbl`이 `border-collapse:collapse`라 셀 `border-image` 인셋이 무시돼 구분선이 카드 끝까지 닿던 문제를, `.tbl-wrap`에 좌우 16px 패딩을 줘 표 전체를 들여써서 해결(더치페이식 — 컨테이너 여백으로 인셋). ② **열 순서 공급가액 → 세액 → 마진** — 기존 공급가액→마진→세액을 상식적 순서로 재배치(colgroup·헤더·행·소계 모두). 집계 값 무변경.
- `v1.96` — 더치페이식 목록·구분선 정리(사용자 피드백 다수). ① **목록을 더치페이 지출 목록식으로** — `.list` 좌우 패딩 제거(0), `.list-item`을 라운드 없는 전체폭 행 + 좌우 16px 인셋 구분선으로. 선택 시 파란 기둥바가 카드 왼쪽 끝에 딱 붙어 흰 여백이 안 생기고, 상호명/품명은 파란색(강조)으로. ② **입출고 내역 일자별 구분선 근본 수정** — `.material-record:last-child`(203·1042 등)가 '일자 그룹의 마지막'까지 지워 1건짜리 날은 구분선이 통째로 사라지던 문제를, `.material-history` 문맥의 높은 특이도 규칙으로 **모든 기록에 인셋 구분선 + 전체의 맨 끝 한 줄만 제거**로 교정. ③ **설정 하단 카드 버튼 정상화** — v1.89의 `.detail .form-actions` 하단 고정바 규칙이 설정 카드 버튼까지 삼켜(버튼이 위에 붙고 아래가 빔) → 규칙을 `#view-quotes/#view-items/#view-companies`로 한정. ④ **사업자등록증 데스크탑 동작** — 다운로드 대신 명함 보내기처럼 클립보드 복사 → 새 탭 열기. 집계·저장 로직 무변경.
- `v1.95` — **견적서 이메일을 모바일에서 이미지 자동 첨부로** — `emailQuote()`가 파일 공유 지원 환경(모바일 등)에서는 `mailto` 대신 **공유 시트(`navigator.share({files,title,text}`)**로 견적 이미지 + 문구를 함께 보낸다. 네이버메일·Gmail·메일 앱을 고르면 이미지가 **자동 첨부**되고 본문에 문구가 채워진다(받는사람 주소는 공유 시트로 지정 불가라 메일 앱에서 입력 — 토스트로 거래처 이메일 안내). 데스크탑 등 파일 공유 미지원 환경은 기존대로 이미지 저장 + `mailto`(받는사람·제목·본문 자동). 헤드리스로 모바일=share(files+text)·데스크탑=save+mailto 확인. 집계·저장 로직 무변경.
- `v1.94` — 사용자 피드백 2종. ① **저장바 아래로 내용 비침 수정** — 데스크탑 sticky 저장바가 스크롤 컨테이너(`.main`)의 *콘텐츠 박스* 기준으로 멈춰, `.main` 하단 패딩(화면폭별 42~48px)만큼 뷰포트 바닥에서 떠서 그 틈으로 비고 등 뒤 내용이 저장바 아래에 비쳐 보이던 문제. `.main`에 `--main-pb` 변수(48/42px)를 노출하고 저장바 `bottom:calc(-1*var(--main-pb))`로 바닥에 딱 붙여 해결(헤드리스 실측 gap 0). ② **사업자등록증을 Dropbox 파일에서 직접 로드** — `shareBizCert()`를 확장: 설정값이 `http(s)`면 링크로, `/`로 시작하면 **앱의 Dropbox 연결로 파일을 직접 내려받아 공유**(`dbxDownloadBlob` 추가). 확장자를 몰라도 되도록 정확 경로 실패 시 부모 폴더를 훑어 파일명 접두어로 매칭(`resolveBizCertFromDropbox`). 기본 경로 `DEFAULT_BIZ_CERT_PATH=/02_Work/문서_THE EDIT/사업자등록증_디에디트(THE-EDIT)02`. 데스크탑 등 공유 미지원 환경은 파일로 저장. ⚠️ `/02_Work/...`처럼 앱 폴더 밖 경로는 **Full Dropbox 권한**이라야 접근된다(App folder 권한이면 못 찾음). 집계·저장 로직 무변경.
- `v1.93` — 견적 목록·품목표 다듬기(사용자 피드백 3종). ① **선택 좌측 기둥바 확대** — 리스트/네비 선택 표시 `::before` 바 3→5px. ② **견적 목록 항목 재구성** — 기존 `상호명 / Q-번호·날짜(회색)`를 → **1줄: 상호명 + 견적번호(옆에 회색 `.li-no`)**, **2줄: 날짜(진한 `--ink-2`·굵기 550 `.li-date`, 발송/납품 정보 포함)**로 변경. 상호명은 ellipsis, 번호는 `flex:0 0 auto`로 안 잘림. 거래처·품목 목록은 기존 `.li-name`/`.li-sub` 그대로(영향 없음). ③ **견적 품목표 헤더 분리** — 합쳐 있던 `색상 · 규격/옵션` 헤더를 데이터 셀의 `.dual`(두 select)에 맞춰 **`색상` \| `규격/옵션` 두 칸**으로 나눠 각 드롭다운 위에 정렬. 집계·저장·출력물 무변경.
- `v1.92` — 견적 상세 납품/수금 카드 다듬기(사용자 피드백 2종). ① **납품 진행률 막대 제거** — 우측에 `7 / 7개 · 납품 완료`로 이미 상태가 보여 중복이던 `.quote-progress` 막대(HTML·CSS·`rate` 계산)를 삭제. ② **카드 내부 간격 확대** — `.quote-flow-body` 상단 여백 추가(0→15px), 수금 합계표(`.payment-totals`) 행 간격 6→12px·글자 13→13.5px·하단 여백 확대, 납품/수금 기록 행(`.quote-record`) 세로 패딩 10→12px. 좁아 보이던 합계 영역이 넉넉해짐. 기능·집계 무변경.
- `v1.91` — 사용자 피드백 3종. ① **견적서 이메일 보내기** — 견적 상세·모바일 미리보기에 `이메일` 버튼 추가. `emailQuote()`가 거래처 이메일(`Company.email`)을 받는사람으로, 공급자명·문서명·번호·일자·합계·유효기간·비고를 본문으로 채운 `mailto:`로 메일 앱을 연다. 메일은 파일 자동 첨부가 안 되므로 견적 이미지를 먼저 저장해 두고 첨부 안내. ② **사업자등록증 보내기** — 상단 `명함 보내기` 옆에 `사업자등록증` 버튼 추가(`shareBizCert()`, 명함과 같은 공유시트→링크복사→열기 3단 fallback). 설정에 `사업자등록증 이미지 링크`(`settings.biz_cert_url`) 입력칸 추가 — 기본 이미지는 없어 링크 미설정 시 설정 화면으로 안내. ③ **일자 입력칸 전부 달력** — v1.40~v1.45의 "값이 있으면 달력 안 열기" 제한을 없애, 견적일·납품일·수금일·기간 등 모든 date/month 칸을 누르면 값 유무와 무관하게 달력이 열린다(사용자 요청). 집계·저장 로직 무변경.
- `v1.90` — 사용자 피드백 2종. ① **품목 선택 팝오버 스크롤 분리 해결** — 견적 품목 선택기(`.ip-pop`)가 `position:fixed`라 화면(또는 데스크탑 `.main` 내부 스크롤러)을 스크롤하면 버튼과 떨어져 떠 보이던 문제를, `window.scroll`을 **capture 단계로 잡아 스크롤 시 팝오버를 닫도록** 처리(내부 스크롤러까지 포착). ② **규격/옵션(사이즈) 순서 변경** — 품목 상세의 옵션 행에 왼쪽 드래그 핸들(`⠿`, `.vhandle`)을 추가하고 `bindVariantReorder()`로 포인터(마우스+터치 통합) 드래그 재정렬 구현. `.vline` 그리드에 20px 핸들 열 추가, 드래그 중 행 강조(`.vdrag`)·놓을 위치 표시(`.vover`), 놓으면 `e.variants` 순서를 바꿔 재렌더. 모바일 가로 스크롤 폭 400→420px. 기능·집계·저장 로직 무변경.
- `v1.89` — 사용자 피드백 4종. ① **업체 자재 입출고 내역 구분선** — 날짜당 1건이면 `.material-record:last-child` 규칙 탓에 선이 안 그려지던 문제를, **날짜 그룹(`.material-day`) 경계에 좌우 16px 여분 일자선**으로 해결. ② **차감(−) 수량 색상**을 더치페이 주황(`--warn-c #FF9500`)으로. ③ **좌측 목록 카드 화면 꽉 채움** — 데스크탑에서 `.main`을 자체 스크롤러(높이 100vh−64px)로 바꾸고 거래처·품목·견적 목록 카드를 sticky 전체높이 + 내부 목록만 스크롤. ④ **상세 폼 저장줄 하단 고정** — `.detail .form-actions`를 sticky bottom으로(견적·품목·거래처 공통) 스크롤 없이 저장 버튼이 항상 보이게. 데스크탑(≥821px) 한정, 모바일 무변경. 기능·집계 무변경.
- `v1.88` — 상세 화면 디테일 다듬기. 견적 상세(`.qt-sec-t`)와 거래처 원장(`.co-ledger-h`) 섹션 제목을 **같은 타이포(13.5px/730·색 통일)**로 맞추고 여백은 문맥별 유지(견적은 `.qt-sec`가 간격 담당, 원장은 자체 여백). 히어로→인라인 스탯 사이 세로 리듬을 살짝 조임(`.scr-hero .sub` 위 9px, `.stats` 여백 15/26·패딩 18). 히어로 라벨에 미세 자간. 순수 CSS·표시만, 기능/집계 무변경.
- `v1.87` — 거래처 원장에 **실제 동작하는 액션 줄** 추가(참조 시안): `수금 등록`(수금/지급 화면으로 이동+거래처 자동 선택), `견적 작성`(새 견적에 거래처 자동 지정), `거래명세서`(그 거래처 최근 견적을 거래명세서 양식으로 열기), `정보 수정`(아래 정보 폼으로 스크롤). 신규 거래처는 원장·액션 없이 폼만. 상세내역 입력 통일 마저 — 옵션·구성품 입력표 헤더(`.vline.head`·`.cline.head`)도 배경 제거해 견적 품목표·표준 표와 톤 일치. 품목 상세·매출 집계는 공통 컴포넌트로 이미 통일됨을 확인(변경 없음). 집계·저장 로직 무변경.
- `v1.86` — **거래처 상세를 원장 형태로 확장**(참조 시안). 거래처 선택 시 상단에 `미수금 잔액 히어로 + 인라인 스탯(납품 누계·수금 누계·거래 횟수·최근 거래) + 거래 내역 표`를 표시하고 그 아래 기존 정보 편집 폼을 `거래처 정보` 구획으로 둠. `coLedger(cid)`가 그 거래처의 납품(`q.deliveries`)·수금/지급(`db.payments`) 기록을 시간순으로 모아 표준 표(`.tbl`)로 렌더(표시 전용, 집계·저장 로직 무변경). 히어로·스탯은 `arData()` 재사용. 신규 거래처(`__new__`)는 원장 없이 폼만. `.scr-hero`·`.stats`·`.tbl` 등 v1.84~85 공통 컴포넌트 재사용.
- `v1.85` — **화면 공통 헤더 통일**(현황·수금·미수금). 각 화면 상단을 `라벨 → 큰 히어로 숫자(30/700) → 부연`(`.scr-hero`) + **인라인 스탯 한 줄**(`.stats`를 카드 그리드에서 좌우 여분 없는 플렉스 라벨/값 행으로 재정의, 히어로와 한 세트)로 통일. 대시보드는 이달 매출 히어로 + 마진·수금·지급·미수금·회신·미발행 인라인, 수금은 순현금 히어로 + 수금·지급, 미수금은 미수금 합계 히어로 + 납품·수금·미납품·미발행. ⚠️ `.stat`이 `<button>`(대시보드)일 때 플렉스 아이템이 전폭으로 늘어 세로로 쌓이던 문제 → `.stat{flex:0 0 auto;width:max-content;max-width:100%}`로 고정. 상세내역 입력(`.qline.head`·`.pay-add`·`.stock-add`)도 배경 없이 통일. 표준 표(`.tbl`)·구분선·선택바(v1.84)와 한 세트. 기능·집계 무변경.
- `v1.84` — **공통 표/구분선/선택 표준 정의**(더치페이 상세내역 양식). ① 표(`.tbl`)는 배경색 없음(헤더·행·hover 모두 투명)·세로 칸선 없음, 헤더 라벨과 데이터 같은 정렬(`.num`=우/`.ctr`=중앙). ② 구분선은 카드 안에서 **좌우 16px 여분을 둔 완전 일자선**(둥근 끝 없음) — 더치페이 `border-image` 그라데이션 기법을 `--div-inset`(16px)로 표준화해 표·`.material-record`에 적용. ③ 선택행(왼쪽 파란 기둥바)은 영수증 앱처럼 **직사각형**(라운드 0). ④ 히어로 숫자 30px/700(`.material-balance-hero`·`.qt-hero-num`). 이후 표·리스트는 이 정의를 재사용할 것. 기능 무변경.
- `v1.83` — 색상 팔레트를 **더치페이와 동일하게 통일**. 파랑 `#4355E8`(soft `#EEEFFE`·txt `#2636B8`), 초록 `#1DAD53`(txt `#13823D`·bg `#EDFAF3`), 빨강 `#E53E3E`(bg `#FFF0F0`), 주황 `#FF9500`, 바탕 `#F7F7F8`·선 `#EBEBED`·글자 `#111/#666/#B0B0B8`로 토큰 교체. 배지도 더치페이 색계로 재매핑 — 매출/수주/입고=초록, 발송/세트=파랑, 매입/작업/출고=주황, 취소=빨강, 납품=초록 채움. 흩어져 있던 옛 색 리터럴(인디고·개별 초록/주황/보라)도 전부 토큰/동일값으로 정리. 기능·레이아웃 무변경.
- `v1.82` — 견적서 작성 화면 레이아웃을 참조 시안 구조로 재구성 + 공통 디테일 통일. 폼 상단에 `견적서·번호 + 상태 알약(eyebrow) → 합계 큰 숫자(hero) → 거래처·총수량·견적일(부연)` 히어로를 추가하고(타이핑 중 합계 실시간 동기화 `fq_heroTotal`), 기본 정보/품목/비고를 얇은 구분선 섹션(`.qt-sec`)으로 묶음. 품목 합계는 회색 박스로 정리하고 합계는 인디고 강조. 공통으로 카드에 은은한 그림자, `.detail` 여백 확대, 페이지 제목 27px, 버튼·입력 라운드(12/11px)를 적용해 다른 화면도 함께 산뜻해짐. 모든 입력 ID·바인딩·기능(인쇄·이미지·공유·납품·수금 패널)은 그대로 유지, 인쇄/캔버스 출력물은 무변경.
- `v1.81` — 색상 토큰만 교체하는 리스킨 1차(레이아웃·기능 무변경). 강조색을 파랑(#2477F3)에서 참조 시안의 페리윙클 인디고(#5B5FE6)로, 바탕을 서늘한 회색(#EDEFF3)으로 바꾸고 의미색(성공/위험/경고)을 부드럽게 조정. 컴포넌트에 하드코딩돼 있던 옛 파랑(`rgba(10,132,255,…)`·`#0868C5`·작업 배지)도 새 인디고로 통일해 견적서 작성 화면부터 일관되게 적용. 폰트는 오프라인 원칙 유지 위해 시스템 스택 그대로.
- `v1.77` — 견적서에 자수·패치 부착 작업이 포함되면 셰프복·유니폼 등 납품 품목 전체 수량에 작업 수량을 자동 연동. 작업 행 수량을 직접 수정하면 자동 연동을 해제해 일부 수량만 작업하는 견적도 유지.
- `v1.76` — 업체 제공 자재 상세를 선불권 보관함처럼 평평한 정보 구조로 정돈. 업체 선택은 상단 가로 목록으로 줄이고 본문 폭을 제한해 잔량·요약·빠른 사용을 집중 배치했으며, 중첩 카드와 회색 날짜 막대를 덜어 날짜별 기록을 얇은 구분선 중심의 내역으로 변경.
- `v1.75` — 업체 제공 자재를 밝은 톤의 마스터-디테일 화면으로 재구성. 보유 업체만 왼쪽에 모으고 선택 업체의 자재별 잔량·받음·사용 합계, 빠른 사용 기록, 상세 입력, 날짜별 수정·취소 이력과 재고내역서를 한 화면에 배치. 모바일에서는 업체 목록을 가로 선택 카드로 전환.
- `v1.74` — 대시보드를 첫 화면으로 바꾸고 메뉴를 현황·업무·기준정보·분석 순으로 재배치. 모바일에는 현황·견적·업체 자재·수금·재고 하단 빠른 메뉴를 추가하고, 대시보드 주요 수치를 관련 화면으로 바로 이동할 수 있는 버튼으로 변경.
- `v1.73` — ERP 전체 UI 개편의 공통 기반을 적용. 카드 그림자와 과한 여백을 줄이고 사이드바·상단바·페이지 제목·목록·폼·표·통계 카드의 크기와 간격을 실무형 밀도로 통일. 기존 기능과 출력물은 유지.
- `v1.72` — 자재 기록 저장·수정·취소 후 버튼 상태를 한곳에서 복구하도록 정리해, 화면에는 보이지만 `수정` 버튼이 계속 비활성화되던 문제 수정.
- `v1.71` — 납품 기록과 진행률은 실제 제품만 집계하고 `작업` 유형의 패치 부착·자수·배송비 및 음수 할인 행은 납품 대상에서 제외.
  기존 납품 기록에 섞인 비대상 행도 표시·집계에서 제외하고 다음 저장 시 정리한다. 매출에서는 작업비·할인액을 최종 납품 회차에 포함해 완납 시 견적 총액과 일치하도록 유지.
- `v1.70` — 매출 집계의 `색상·규격/옵션`을 색상과 규격/옵션 독립 열로 분리하고, 현재고·입출고 기록까지 같은 품목 표 열 구분 규칙으로 통일.
  입출고 기록에서도 품목에 합쳐 보이던 색상과 규격/옵션을 각각 표시하며, 표의 세로 구분선·행 강조·모바일 가로 스크롤 폭을 일관되게 조정.
- `v1.69` — 납품 기록 입력에 `남은 수량 전체 입력`을 추가해 품목별 미납 수량을 한 번에 채운 뒤 검토·저장할 수 있게 개선.
  업체 제공 자재 기록은 일자별 그룹으로 정렬하고 받음/차감과 수량을 색상·굵기로 구분한다.
  수정·기록 취소 버튼을 각 행 오른쪽에 고정하고 행 간격과 입력 폼 높이를 줄여 데스크톱·모바일 가독성을 개선.
- `v1.68` — 지급자재 재고내역서와 자재 입출고 기록을 별도 작업 영역으로 재구성.
  내역서와 사용·반환·불량 입력에는 현재 잔량이 있는 업체만 표시하고 하나뿐이면 자동 선택한다.
  기존 자재 종류를 잔량과 함께 선택하며 작업 차감 후 예상 잔량을 즉시 표시한다. ‘받음’에서는 전체 업체와 새 자재명을 허용한다.
  기록 내역에서 수정·수정 취소·수정 저장을 지원하며 전체 원장을 재검증한 뒤 저장해 과거 잔량 부족과 저장 실패 시 원본을 보존한다.
  조회 기준일은 과거 시점 내역서용, 기록일자는 실제 입출고일로 명확히 안내한다. 업체 미선택 안내와 iOS 미리보기 화면 높이·안전 영역·스크롤 잠금도 보강.
- `v1.67` — 업체 제공 자재의 지급자재 재고내역서 미리보기·인쇄/PDF 저장 추가.
  선택 거래처·기준일의 현재 유효한 저장 기록만 집계하여 자재별 받음/사용/반환/불량·분실/잔량,
  일자순 상세 증감·자재별 잔량을 견적서 스타일로 출력한다. 내부 메모·주문번호·금액은 제외.
  보관 업체 정보는 설정을 사용. 거래처 간 혼입·미저장/취소/기준일 이후 내역을 제외하고 원본은 변경하지 않는다.
  견적 미리보기의 이미지·공유 기능은 유지하며 내역서는 인쇄/PDF만 제공한다.
  집계·개인정보 제외·기준일 회귀 테스트 통과. 실제 모바일·다중 페이지 인쇄 검증은 별도 필요.
- `v1.66` — 업체 제공 자재 메뉴와 별도 `material_moves.json` 수량 원장 추가.
  거래처·자재명별 소유 재고를 분리하며 받음/작업 완료/반환/불량·분실 이력을 기록한다.
  작업 수량 × 작업당 사용량으로 차감. 스마트스토어 등 주문 경로·주문번호·회차 메모를
  견적 없이 등록 가능하며 견적 연결은 선택 사항이다. 매출·수금·회사 재고는 생성하지 않는다.
  자재 차감의 유일한 트리거는 이 메뉴의 작업 완료 저장이며 견적·납품에서 중복 차감하지 않는다.
  잔량 부족·과거 날짜 수량 부족·중복 주문 경고, 기록 취소 이력과 저장 실패 보존을 제공한다.
  새 테이블을 로딩·가져오기·백업에 포함. 자재 이력이 있는 거래처 삭제와 이력이 없는 옛 백업 복원을 차단.
  `tests/materials.cjs` 및 기존 회귀 테스트 통과. 실제 브라우저·모바일 레이아웃 확인은 별도 필요.
- `v1.65` — 거래처 `prices:[{item_id,color,spec,price}]` 약정 판매 단가 추가 (부가세 별도).
  색상·규격 모두 일치 → 규격 → 색상 → 제품 전체 → 품목 기본가 순으로 적용.
  견적 품목 선택·옵션 일괄 입력·거래처/옵션 변경에 연결하며 직접 입력한 가격은 유지.
  견적 행 `price_auto`가 true일 때만 재계산하고 기존 견적·납품 행은 보존한다.
  약정 중복/음수 검증과 거래처 저장 실패 시 데이터·입력 보존. 약정 변경은 거래처 저장으로만 확정.
- `v1.64` — 품목 유형에 `작업`을 추가. 자수·패치 부착 같은 작업형 품목은
  견적·매출에는 포함하되 재고 현황·재고 부족·자동 출고 계산에서는 제외.
- `v1.63` — 설정 화면에 다른 컴퓨터·휴대폰에서 이어 쓰기 안내를 추가하고,
  ERP 주소 복사 버튼과 Dropbox 연결 다시 설정 버튼을 제공.
- `v1.62` — 기본 App folder 키로 빈 Dropbox 앱 폴더를 보고 있을 때 정상 동기화처럼 보이지 않도록
  상단 동기화 상태와 견적서 빈 화면에 데이터 위치 확인 안내, Dropbox 재연결 버튼을 표시.
- `v1.61` — 모든 날짜·월 입력을 공통 표시 래퍼로 통일하고 모바일 Grid 최소 너비를 제한.
  네이티브 날짜 선택은 유지하면서 Safari 고유 표시 크기를 레이아웃에서 분리한다.
  견적별 회차·품목 수량별 분할납품과 직접 입금 등록, 부분납품 상태, 납품분 월별 집계 추가.
  기존 견적은 납품 기록으로 정규화하고 이전 재고 정책을 보존하여 중복 차감을 방지한다.
  신규 견적은 납품 수량으로 재고 반영하며 납품·입금 연결 견적 삭제를 방지한다.
  검증: `tests/quote-workflow.cjs` 통과. `tests/date-layout.cjs`는 브라우저 실행 제한으로 미검증;
  실제 iPhone Safari 확인 필요. 라이브 반영은 사용자 병합 승인 후 진행.
- `v1.60` — iPhone Safari에서 견적일자 입력칸이 카드 밖으로 넘치는 문제를 수정.
  모바일 견적일·납품일·세금계산서 발행일 입력칸의 너비를 컨테이너 안으로 제한한다.
- `v1.59` — 모바일 견적서 하단 작업 영역을 재배치. 문서 양식과 **저장/등록** 버튼은
  가로 전체 폭으로 표시하고, 복사·인쇄·이미지·공유는 2열, 삭제·취소는 마지막 줄에 분리한다.
- `v1.58` — 견적서 날짜 필터에 **전체 기간** 버튼을 추가. 상태와 검색어는 유지하면서
  시작일·종료일만 한 번에 해제해 모든 날짜의 견적서를 바로 볼 수 있다.
- `v1.57` — 견적서 화면을 반응형 마스터-상세 구조로 수정. 필터를 목록 카드 안에 고정해 모바일에서도
  항상 보이게 하고, 모바일은 목록↔상세 전환, 데스크톱은 왼쪽 목록+오른쪽 상세로 표시한다.
  모바일 목록 복귀 버튼과 필터 결과 요약을 추가.
- `v1.52` — 견적서의 **저장하지 않은 변경사항 경고**, 상태·시작일·종료일 필터와 초기화,
  상단 Dropbox 저장·동기화 상태(진행·완료 시각·실패)를 추가. 필터 결과는 CSV에도 동일하게 적용.
- `v1.51` — **견적서 복사** 추가. 기존 견적의 거래처·품목·수량·단가·비고를 새 작성 화면으로
  가져오되 견적일은 오늘, 상태는 작성중으로 초기화하고 납품일·세금계산서 발행일은 비운다.
  복사 직후에는 저장하지 않으며 등록할 때 새 견적번호를 발급한다.
- `v1.47` — Dropbox 저장 위치를 `/07_Apps/거래처관리(ERP)/`로 이동(앱 전체 폴더 통합).
  App key를 로그인 화면에서 입력받도록 변경(Full Dropbox 앱으로 교체하기 위함) + 옛 `/erp` 읽기 폴백.
- `v1.0` — 거래처 관리 ERP 초기 배포
- `v1.1` — 품목·견적서(인쇄)·수금/지급·대시보드·공급자 정보 구현
- `v1.2` — 견적서 인쇄 양식을 실사용 양식(디에디트)으로 교체 — 유효기간·계좌번호 필드 추가
- `v1.3` — 견적서 그레이 디자인(레이아웃 고정) + 품목 사이즈 옵션·세트 구성
- `v1.4` — 품목 색상(BK/WH) + 재고 관리 + 수주 시 자동 출고
- `v1.5` — 견적서 합계 행 열 정렬(라벨·총액 제거) + 대시보드 버전 표시
- `v1.6` — 매출 집계: 기간·거래처별 품목 집계 + CSV 내보내기 (세금계산서 준비용)
- `v1.7` — 견적서 기타란에 주문 제작 안내 문구 빨간색 고정 표시
- `v1.8` — 품목 + 버튼 클릭 시 이전 편집 내용이 남는 버그 수정 (빈 폼으로 초기화)
- `v1.9` — 버전 표시를 상단 ERP 로고 옆으로 이동 + 로고 클릭 시 새로고침
- `v1.10` — **모바일 대응**: 햄버거 드로어 내비게이션(≤820px에서 사이드바가 숨겨져 화면
  이동 자체가 불가능했음) + 목록 선택 시 상세 자동 스크롤 + 재고·매출 집계 모바일 레이아웃 정리
- `v1.11` — 모바일 견적서 인쇄 미리보기 오버레이 (인앱 브라우저에서 `window.print()` 무반응 대응)
- `v1.12` — 견적서 인쇄 2페이지 분리 수정 (미리보기 여백이 인쇄에 적용돼 A4를 넘김)
- `v1.13` — 모바일 인쇄 레이아웃을 데스크탑과 동일하게 (인쇄 폭 688px 고정, iOS 링크 밑줄 차단)
- `v1.14` — **견적서 이미지(PNG) 내보내기** — 인쇄 양식과 동일한 레이아웃을 Canvas에 2배 해상도로 그림
- `v1.15` — 매출 집계 테이블 열 정렬 통일 (`table-layout:fixed` + `colgroup`)
- `v1.16` — 매출 집계 소계 행 굵은 글씨로 시안성 개선
- `v1.17` — 모바일 매출 집계 겹침 수정 (테이블 min-width 860px, 카드 상단 요약 줄바꿈)
- `v1.18` — 홈 화면 앱 아이콘 추가 (apple-touch-icon·manifest·favicon, 상단바 로고와 동일 디자인)
- `v1.19` — **견적서 공유 기능** — 공유 시트 → 클립보드 복사 → 파일 저장 3단 fallback.
  '이미지' 버튼은 항상 파일 저장으로 역할 분리
- `v1.20` — 견적서 목록 CSV 내보내기 (검색 결과 그대로, 견적 1건당 1행).
  `filteredQuotes()`로 목록 화면과 CSV가 같은 필터를 공유
- `v1.21` — 전체 데이터 JSON 백업·복원 (설정 탭). 형식 검증 후 전체 덮어쓰기
- `v1.22` — **거래처별 미수금** 뷰 추가 — 수주 견적 합계 − 수금 합계, CSV 내보내기 포함.
  카드 총액은 전체 기준, 표 하단은 표시된 행 기준이라 라벨을 '합계/표시 합계'로 구분
- `v1.23` — **거래명세서 양식** — `DOC_TYPES`로 견적서/거래명세서 전환.
  인쇄·이미지·공유 3경로 모두 선택된 양식으로 출력
- `v1.24` — **PWA 오프라인 지원** — 서비스워커 추가.
  HTML network-first(배포 즉시 반영) + 정적 자원 cache-first
- `v1.25` — 모바일 공유 시 이미지 파일만 전송 — `navigator.share()`의 `text`·`title` 제거
  (카톡에 요약 메시지가 함께 발송되던 것 삭제)
- `v1.46` — **BOM(소요량) 강화** — 세트를 원자재까지 **다단계 재귀 전개**(`expandBom`).
  중첩 세트의 가격·재고 차감이 통째로 누락되던 문제 수정(개업패키지 가격 10,000→80,000원).
  순환 참조 차단. 견적 폼에 **재고 부족 경고**(필요·보유·부족), 품목 상세에 1세트당 소요 자재 표시.
  `quoteStockDone(null)`이 수동 입출고를 잡아 보유량이 0으로 보이던 버그 수정
- `v1.45` — 날짜 칸 달력 자동 열기를 **빈 칸에만** 적용. v1.40이 모든 날짜 칸에서 클릭 시
  달력을 열어 **직접 타이핑이 막혀 있었다** (달력이 열리면 키 입력을 달력이 가져감).
  값이 있는 칸은 기본 동작 → 숫자로 직접 수정 가능, 달력은 오른쪽 아이콘으로.
  모바일(iOS)은 원래 탭하면 달력이 뜨므로 영향 없음
- `v1.44` — 출력물 **규격·수량 가운데 정렬** (사이즈·수량은 자릿수가 짧아 가운데가 읽기 쉬움).
  캔버스도 `ALIGN` 배열 + `cell()` 헬퍼로 정리해 인쇄 표와 규칙을 공유
- `v1.43` — 출력물의 **색상과 규격을 별도 열로 분리** (기존엔 "WH · L"로 한 칸에 합쳐 표시하면서
  헤더는 "규격"이라 무엇이 무엇인지 알기 어려웠음). 인쇄·캔버스 모두 7열로,
  열 폭은 실측 기준으로 재배분. 품목 폼에 규격·사이즈 관계 안내 추가
- `v1.42` — **ROADMAP 3단계 ③: 수금 ↔ 견적 연결** — `Payment.quote_id` 추가.
  수금 입력 시 그 거래처의 '납품했고 덜 받은' 견적을 골라 연결(고르면 잔액이 금액칸에 자동 입력),
  지급은 대응 문서가 없어 비활성. 미수금 화면에 **건별 보기** 추가 —
  견적마다 납품액·수금액·잔액·계산서 상태. 건에 연결되지 않은 수금은 하단에 따로 표시해
  숫자가 맞아떨어지게 함. 수금/지급 목록에도 연결 견적번호 표시.
  `#payAmt`·`#ivQty`도 text+쉼표로 통일 (number 타입이라 쉼표 값이 무효 처리되던 문제)
- `v1.41` — **ROADMAP 3단계 ②: 세금계산서 발행 체크** — `Quote.tax_at` 추가.
  견적 폼에 발행일 입력('오늘' 버튼), 납품했는데 미발행이면 목록에 '계산서 미발행' 배지.
  미수금 화면에 미발행 금액·건수 카드와 거래처별 컬럼, 대시보드에도 미발행 건수.
  `needsTax(q)` = 납품됨 && 미발행 (미납품 건은 대상 아님)
- `v1.40` — **날짜 칸 아무 데나 누르면 달력이 열린다** — `showPicker()`를 document에
  한 번만 위임 등록해서 date/month 입력 전체(견적일자·납품일·수금일·월·기간·재고일)에 적용.
  나중에 추가되는 날짜 칸에도 자동 적용된다 (receipt-db와 같은 방식)
- `v1.39` — 사이즈별 패널이 **견적에 이미 들어간 품목**을 기본으로 잡는다
  (여러 개면 품목 행 맨 위의 것, 색상도 그 행 기준). 없을 때만 전체 품목 중 첫 번째.
  다른 견적으로 옮기면 패널을 닫는다
- `v1.38` — 수량·단가 입력을 `type=text`로 바꿔 정렬 정확화(스피너 제거),
  **입력칸 천단위 쉼표**(포커스 시 해제·블러 시 복원), 단가 헤더를 숫자 위치에 맞춤,
  `select()` 동기 호출로 Tab 이동 누락 수정
- `v1.37` — **ROADMAP 3단계 ①: 납품일 도입** — `Quote.delivered_at` + '납품' 상태 추가.
  미수금을 **납품액 − 수금액**으로 정정하고 미납품 잔액을 분리 표시.
  대시보드 매출도 납품 기준. 기존 수주 건은 1회 마이그레이션으로 납품 처리해 수치 보존
- `v1.36` — 사이즈별 입력에서 **Tab이 단가를 건너뛰고 옆 사이즈 수량으로** 이동.
  패널을 열거나 품목·색상을 바꾸면 첫 사이즈 수량에 자동 포커스, 포커스 시 기존 값 전체 선택.
  마지막 사이즈에서 Tab을 누르면 '품목에 넣기' 버튼으로. 단가는 클릭으로 계속 수정 가능
- `v1.35` — **품목코드 + 견적서 품목 검색** — `Item.code` 추가(이카운트 방식).
  견적 품목 선택을 `<select>` → **검색형 선택기**로 교체 (품명·코드 검색, ↑↓·Enter, 바깥 클릭 닫기).
  견적 행의 `name`에는 `itemLabel()`로 **"품명 [코드]"**를 넣어 기존 출력물 표기를 유지.
  품명 끝의 `[XXX]`를 코드로 분리해 주는 안내(기존 데이터 이전용) 포함
- `v1.34` — 견적 품목 행 열별 정렬 — 단가는 금액과 같이 오른쪽, 수량·규격/사이즈는 가운데
- `v1.33` — 견적 품목 행의 **규격/사이즈 칸 확대** (104px → 176px). 색상·사이즈 select 두 개가
  좁은 칸에 들어가 "WH"가 "W"로 잘리던 문제. 품목 칸이 과하게 넓어(1.4fr → 1fr) 남는 폭을 넘김.
  두 select만 글자 12.5px·좌우 여백 축소. 모바일 가로 스크롤 기준 560 → 620px
- `v1.32` — **사이즈별 한번에 입력** — 같은 품목의 여러 사이즈를 행 추가 없이 한 화면에서
  수량만 채워 넣는 패널(`renderSizePanel`/`applySizePanel`). 기존 행의 수량을 그대로 보여줘
  수정도 가능하고, 0/빈칸이면 해당 행을 제거. 품목·색상 단위로 동작
- `v1.31` — **대시보드 재구성** — 현금 입출금만 보이던 것을 매출(수주)·마진·수금·지급·미수금·
  회신대기까지 담은 사업 현황 화면으로. 월별 차트도 수금·지급 → **매출·수금**으로 변경
- `v1.30` — 견적 **총 수량** 표시 (사이즈별로 나눠 입력할 때 합계가 안 보이던 불편 해소) +
  견적 발송일(`Quote.sent_at`) 기록과 '발송 N일째' 배지 + 거래처 매출/매입 구분 필터 +
  수금/지급 화면에 거래처별 집계 카드
- `v1.29` — **품목 매입단가 살리기** — `itemBuy()` 추가, 품목 상세 1개당 마진,
  견적 예상 마진(내부용), 매출 집계 마진 컬럼·카드·CSV
- `v1.28` — 견적서·거래명세서 하단 라벨을 "기타" → **"비고"**로 통일
  (입력란은 비고인데 출력물만 기타여서 용어가 어긋나 있었음). 인쇄·캔버스 양쪽 변경.
  수금/지급의 결제수단 "기타"는 그대로 유지
- `v1.27` — **ROADMAP 1단계: 정합성 복구** — ① 재고를 reconcile 방식으로 재설계
  (수주↔취소 반복·수량 증감·재저장·삭제 모두 정확, `stock_moves.quote_id` 추가 +
  기존 데이터 마이그레이션) ② 견적번호를 최대값 기준 채번으로 (삭제 후 중복 해소)
  ③ 거래처 삭제 시 연결된 견적·수금 건수 경고
- `v1.26` — **거래처별 견적서 기본 비고** (`Company.quote_memo`) — 거래처 선택 시 견적 비고에
  자동 입력. `applyCoQuoteMemo()`가 **직접 입력한 내용은 덮어쓰지 않고**, 비었거나 직전 거래처의
  기본 문구 그대로일 때만 교체 (receipt-db의 `_catAutoFilled`와 같은 방식)

## 다음 작업 후보
- 미수금에 연령 분석(30/60/90일 경과) 추가
- 수금 입력 시 특정 견적서와 연결 (현재는 거래처 단위 상계)
- 품목별 매입·매출 마진 분석
- 견적서 상태 변경 이력 (작성중 → 발송 → 수주 타임스탬프)
- 다중 사용자 공유 (Dropbox 공유 폴더 가이드)
